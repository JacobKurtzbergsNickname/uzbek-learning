# TODO: Replica Set

If you need transactions, change streams, or plan to scale to multiple nodes, re-enable replica set mode in your Docker Compose setup.

Current setup is single-node MongoDB without replica set for simplicity.
